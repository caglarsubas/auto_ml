push policy:

named as: feature/vx.y.z-date-description
x: core function changes/additions
y: subsidiary function changes/additions
z: bug-fix or hot-fix


!CFH!
- make feature-names clickable in SFS result tables where clicks open the feature-card modal we have the importance and explainability tabs in feature-card should be updated according to the completed step (first we fill these tabs in start-modeling button click, then we will fill them by running SFSs). So, when a user clicks a feature-name and opens a feature-card, first two tabs (description and quality) never changes, but the other two (importance and explainaiblity) should correspond the step result in which the user open the feature-card via its table.
- advanced configuration menu should appear for forward selection being run consecutively after a completed backward sfs.
- enhance the test framework in the aim of covering recent codebase developments
* make pipeline interface witdh-adjustable 3 layers layout (left progress navigation (cancel current hidden-sliding one), middle pipeline-flow, right LLM based assistant chat (for now openai gpt-5.3)). add a button named 'Get AI Support' after each process output (data quality summary, categorical feature encoding, Modeling-Cross-Validation, Modeling-SHAP Beeswarm (interactive), Modeling-Selected Features (sorted by Combined Score), SFS Results) providing insights about the output being showed to the customer and answers her/him technical questions about the results, make reasonable, advisory comments. 
- in a pipeline run, user should be able to go backward (revert) between steps. for example, while in the modelling step user can walk 1 step back and can change the configuration of preprocessing step and restart it

- start hyperparameter
- add 'view details' button right under the progress-bar of SFS-running, when clicek user should see the selected feature, its description, cv based performance metric of model including the new feature, percentage change of performance metrıc as ın table format:
- stopping criteria selections via %-change of metrics and min-max feature count.
- make 3 layers layout (left progress navigation, middle workspace, right assistant chat)
- add causality features into explainability tab
- add nonlinear VIF if necessary


While the implementation is complete and functional, here are potential future improvements:
- Export Results - CSV/Excel export of SFS results
- Plot Performance Curves - Line charts showing metric progression
- Feature Comparison - Side-by-side before-adding/after-adding analysis
- Custom Scoring - Allow custom scoring functions adding via UI
- Parallel Execution Option - Run forward/backward simultaneously
- History Tracking - Save multiple SFS runs for comparison


