push policy:

named as: feature/vx.y.z-date-description
x: core function changes/additions
y: subsidiary function changes/additions
z: bug-fix or hot-fix


!CFH!

- (ongong)each plot in feature-impact details modal which is being opened via view buttons of details column in sfs result table can be full screen individually
- (waiting in queue) for the attached pipeline being saved, we are currently at Modeling-SFS step but the saved-pipelines menu shows it as in modeling step. we should implement a more granular status illustration for pipelines. this is the major and minor step taxonomy you should use in checkpiont saving schema:
1- Declaration
1.a- Pipeline Declaration
1.b- Data Declaration
1.c- Dictionary Declaration
2- Preprocessing
2.a- Data Purifier Declaration
2.b- Data Quality Summary
3- Modeling
3.a- Categorical Feature Encoding
3.b- Modeling (Start Modeling button)
3.c- SFS
3.c.i- SFS Backward (if firstly run before the consecutive forward selection run) 
You can use these hierarcy and taxonomy for naming the status of pipelines in saved-pipeline list

- user should be able to add commentary lines. between each pre-located objects (figures, tables, menus, etc.) in the pipeline in the aim of taking decision notes about the flow for future investigations. these notes should saved as well in our autosaving or manuel-saving mechanism.
- there should be a stop sfs button under the progress bar. when pushed we should show the completed steps and the results depending only on those completed steps. then user can be click the continue button where we should keep moving the paused process of SFS from where it was being stopped
- currently each pipeline run is stateless. hence user cannot continue where she/he left if wants to make a pause of the flow. Each pipeline run should be saved with a unique name (pipeline-type + timestamp) that can be changed by user and add checkpoints on every step completion that user can quit and come back to continue from the last checkpoint
- in a pipeline run, user should be able to go backward (revert) between steps. for example, while in the modelling step user can walk 1 step back and can change the configuration of preprocessing step and restart it
- we should show the train-test split validation to the user who is expecting the declared distribution. show with a wise illustration (can be stacked histogram where stackes we have target labels, so user can easily understand that target mean is not changing over splitted datasets)

- start hyperparameter
- add 'view details' button right under the progress-bar of SFS-running, when clicek user should see the selected feature, its description, cv based performance metric of model including the new feature, percentage change of performance metrıc as ın table format:
- stopping criteria selections via %-change of metrics and min-max feature count.
- make 3 layers layout (left progress navigation, middle workspace, right assistant chat)
- add causality features into explainability tab
- add nonlinear VIF if necessary


While the implementation is complete and functional, here are potential future improvements:
- Export Results - CSV/Excel export of SFS results
- Plot Performance Curves - Line charts showing metric progression
- Feature Comparison - Side-by-side before-adding/after-adding analysis
- Custom Scoring - Allow custom scoring functions adding via UI
- Parallel Execution Option - Run forward/backward simultaneously
- History Tracking - Save multiple SFS runs for comparison


