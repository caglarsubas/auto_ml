Django>=5.1,<5.2
djangorestframework>=3.15,<3.16
pandas>=2.2
numpy>=1.26
scipy>=1.11
openpyxl>=3.1
xlrd>=2.0
python-magic>=0.4
django-cors-headers>=4.3
scikit-learn>=1.4

# Modeling dependencies
xgboost>=2.0,<3.0
lightgbm>=4.0,<5.0
catboost>=1.2,<2.0
optuna>=3.6,<5.0
shap>=0.42
matplotlib>=3.7
mlxtend>=0.23
statsmodels>=0.14

# AI Assistant
openai>=1.0
# retrieval.namespace (SDK #82) + embeddings.create (SDK #80) shipped in
# 0.20.2 on PyPI, so the interim commit-archive pin is retired as its comment
# always intended. That URL is now unfetchable anyway — the source repo 404s,
# which broke `pip install -r` (and therefore all of CI) on every branch.
prometa-sdk>=0.20.2
redis>=5.0
mcp>=1.27,<2
cryptography>=42
chromadb>=0.5,<1.1

# Testing dependencies
pytest>=8.0
pytest-django>=4.8
pytest-html>=4.1
pytest-cov>=5.0
pytest-timeout>=2.3
ruff>=0.9
